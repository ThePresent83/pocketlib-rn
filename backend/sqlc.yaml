version: "2"
sql:
  - engine: "postgresql"
    schema: "atlas/schema.sql"
    queries: "db/query"
    gen:
      go:
        package: "sqlc"
        out: "src/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_empty_slices: true
